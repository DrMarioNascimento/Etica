name: Rebuild vinculo HQ
on:
  push:
    paths:
      - 'dilema-bonde-mobile/assets/.vinculo_chunks/READY'
permissions:
  contents: write
jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Rebuild SVG
        shell: bash
        run: |
          cat dilema-bonde-mobile/assets/.vinculo_chunks/part-* > dilema-bonde-mobile/assets/vinculo.svg
          rm -rf dilema-bonde-mobile/assets/.vinculo_chunks
          rm -f .github/workflows/rebuild-vinculo-hq.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dilema-bonde-mobile/assets/vinculo.svg dilema-bonde-mobile/assets/.vinculo_chunks .github/workflows/rebuild-vinculo-hq.yml
          git commit -m 'Replace vinculo with high-fidelity transparent SVG'
          git push
